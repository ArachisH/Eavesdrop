name: Test

on:
  workflow_run:
    workflows: [Restore]
    types:
      - completed

jobs:
  on-success:
    runs-on: self-hosted
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: dotnet test --verbosity n --no-restore
